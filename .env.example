# API Configuration for baseline inference (/baseline and inference.py)
# Keep real values in Hugging Face Space Variables (never commit secrets).

HF_TOKEN=your-huggingface-or-provider-api-key
API_BASE_URL=https://router.huggingface.co/v1
MODEL_NAME=Qwen/Qwen2.5-72B-Instruct

# Optional local alias for compatibility
API_KEY=

# Benchmark name
CYBER_SOC_BENCHMARK=cyber-soc-env
